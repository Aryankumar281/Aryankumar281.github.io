// arrow function
const double = (a, b) =>{
    return a + b;
}
double(5, 7);
// add is an arrow function that takes two parameters a and b.
// It returns the sum of a and b.
// The return is implicit (no need to write return or use {} when it's a single expression).